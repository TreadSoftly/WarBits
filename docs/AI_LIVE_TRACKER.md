# AI Live Tracker — WarBits / WarBytes

This file is a **shared, live coordination log** used by:

- ChatGPT (this chat)
- VS Code Codex (your editor agent)

It is **not** meant for end users. It is meant to keep the AIs focused, coordinated, and measurable.

---

## How to use this file (rules)

- Always record **evidence** for claims: command output snippets; file/line references; benchmark logs.
- Avoid “it feels better” without numbers. Prefer `render_avg_ms`, `fps_avg`, stage timers; artist counts (`collections`, `lines`).
- Keep changes **behind flags** unless they are pure bug fixes.
- Do not change sim outcomes in renderer code (renderer must be “read-only” on sim state).

---

## Current baseline and branches

- **Baseline (WarBits v1)**: Matplotlib primitives (dots/triangles/boxes).
  Goal: golden, portable, reproducible baseline.

- **Branch (WarBits v1.2)**: Matplotlib + STL visuals (Codex-owned).
  Goal: STL visuals without breaking determinism; performance as good as possible within Matplotlib constraints.

- **Future (WarBytes v2)**: Panda3D GPU renderer.
  Goal: match baseline behavior and camera semantics; move heavy visuals to GPU.

---

## Status summary (auto-generated)

> This section is generated by `tools/update_ai_live_tracker.py` from `docs/ai_live_tracker.yaml`.

- Last updated: **2026-02-05**
- Overall completion: **(run updater to compute)**
- P0 completion: **(run updater to compute)**
- P1 completion: **(run updater to compute)**
- P2 completion: **(run updater to compute)**

---

## Latest test results (paste logs here)

Date:
Commit/tag:
Command:
Results (fill these fields):

```text
fps:
render_avg_ms:
sim_step_ms:
artist counts:
```

Notes:

---

## Open blockers (keep short)

- [ ] (example) Repo audit bundle not yet posted here
- [ ] (example) Stage timers not merged into baseline
- [ ] (example) Camera top-down mode not default yet

---

## Workstreams (single source of truth)

> For the authoritative list + % completion, edit `docs/ai_live_tracker.yaml` and run the updater.

- W0: Repo health + packaging
- W1: Matplotlib render perf + instrumentation
- W2: Camera modes + stability
- W3: Terrain realism + LOD separation (physics vs render)
- W4: Weapons cadence + scheduling robustness
- W5: Target selection + next/prev cycling + HUD highlight
- W6: Determinism + headless bench harness
- W7: STL Matplotlib branch guardrails (decimation, caching, batching)
- W8: Panda3D migration readiness (renderer seam, typed events)

---

## AI-to-AI notes (append-only)

### ChatGPT notes

- 2026-02-05: Initialized live tracker templates + updater script. Waiting on repo code visibility OR audit bundle logs.

### Codex notes

- (Codex writes here)
